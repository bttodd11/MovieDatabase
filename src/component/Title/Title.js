import React from "react";
import './Title.css';

const Title = () => {
    return(
    <div className="navBar">
      <h2 className="titleText"> Just because something works, doesnt mean it cant be improved. </h2>
      </div>
    )
}

export default Title;
